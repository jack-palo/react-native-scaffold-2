import {
  Alert as _Alert,
  AlertIcon as _AlertIcon,
  AlertText as _AlertText,
  styled,
} from '@gluestack-ui/themed'

/**
  Alert example:
  <Alert>
    <AlertIcon as={InfoIcon}/>
    <AlertText>sample</AlertText>
  </Alert>
 */
export const Alert = styled(_Alert)
export const AlertIcon = styled(_AlertIcon)
export const AlertText = styled(_AlertText)
